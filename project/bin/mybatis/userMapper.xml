<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="LegendCarry_Improve.mybatis.userMapper"> 

	<!-- 配置添加方法 -->
	<insert id="add" parameterType="User">
		insert into user values(#{username},#{password},#{level},#{grade},#{time},#{maxgrade},#{blood})
	</insert>
	
	<!-- 配置更新方法 -->
	<update id="update1" parameterType="User">
		update user set level=#{level},grade=#{grade} where username=#{username}
	</update>
	
	<update id="update2" parameterType="User">
		update user set time=#{time} where username=#{usermane}
		update user set blood=#{blood} where username=#{username}
		update user set maxgrade=#{maxgrade} where username=#{username}
	</update>
	
	<!-- 配置查询方法 -->
	<select id="check" parameterType="int" resultType="User">
		select * from user where username=#{username} and password=#{password} 
	</select>
	
	<select id="get" parameterType="int" resultType="User">
		select * from user where username=#{username}
	</select>

</mapper>